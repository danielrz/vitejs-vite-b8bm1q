export interface Props {
  children?: React.ReactNode
  interval: number
}

export interface GetItemsServiceInput {
  term: string,
  interval: number
}
export interface UseSearchInput {
  term: string,
  interval: number
}
